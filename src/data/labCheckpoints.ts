/**
 * One checkpoint per lab step: a short question about what that step does, what to enter, or what
 * you should see when it worked.
 *
 * A walkthrough with only a "Proceed" button can be clicked through in a minute and teaches nothing,
 * which is how the labs felt to use. Most learners also do not have a Firebox in front of them. A
 * checkpoint makes every step do something either way: with hardware it confirms you know what the
 * result should look like, and without it you are still rehearsing the menu paths, values and
 * verification checks the exam asks about.
 *
 * Every answer is taken from that step's own instruction in labs.ts or from Fireware behaviour the
 * step depends on, so a checkpoint never teaches something the lab does not. Rows are
 * question | answer | three distractors | why, in the same shape as the authored question bank.
 * Keys are `${labId}.${stepNumber}`; labCheckpoints.test.ts requires exactly one per step.
 */

export interface LabCheckpoint {
  question: string;
  answer: string;
  options: string[];
  why: string;
}

const ROWS = `
1.1 | After cabling a factory-default Firebox, which address range should the management PC lease on the trusted interface? | 10.0.1.0/24 | 192.168.1.0/24 | 169.254.0.0/16 | 10.0.100.0/24 | A factory-default Firebox serves DHCP on its trusted interface in 10.0.1.0/24, so a lease there proves the cable and interface are right. A 169.254 address would mean no DHCP offer arrived at all.
1.2 | In WatchGuard System Manager, where do you start the Quick Setup Wizard? | Tools > Quick Setup Wizard | File > Connect to Device | Setup > Global Settings | Help > About WatchGuard | The wizard lives under Tools. It discovers the factory-default Firebox and walks through naming it, the external interface, and the trusted interface at 10.0.1.1/24 with DHCP.
1.3 | Why does the wizard ask for two different passphrases? | The status account is read-only; admin can change settings | One encrypts the backup file, the other unlocks the feature key | One is used by the Web UI and the other only by the CLI | One protects DNS settings and the other protects DHCP | The status and admin accounts are separate built-in accounts with different privileges. Distinct passphrases mean handing someone read-only access never hands them the ability to change the configuration.
1.4 | Which role suits an account that should view status and logs but never change the configuration? | Device Monitor | Device Administrator | Guest Administrator | Mobile VPN user | Device Monitor is read-only. Giving each person their own account instead of sharing status or admin also means the audit trail shows who did what.
1.5 | After enabling Always Create a Backup and saving again, what should be in the WatchGuard configs folder? | The config file plus an automatic backup of it | Only one file, which is overwritten on every save | A Fireware OS installer for the current release | An exported copy of the Traffic Monitor log | The automatic backup is your way back if a later save goes wrong. Checking the folder proves the option actually took effect, rather than assuming it did.
2.1 | Where do you read the Firebox model and Fireware OS version after connecting in WatchGuard System Manager? | The Device Status tab | Help > About WatchGuard | The Traffic Monitor tab | Setup > Global Settings | Device Status shows the connected Firebox's model and OS version. Help > About WatchGuard shows the build of WatchGuard System Manager itself, which is separate software that must be compatible with it.
2.2 | What is the quickest way to reach the help page for one specific policy in Policy Manager? | Open that policy and use its dialog's Help button | Search the Traffic Monitor for the policy's name | Run the Quick Setup Wizard again from Tools | Open the Firebox Status Report and search for it | Help inside a dialog is context-sensitive, so it opens the reference page for that exact policy instead of the start of the manual.
2.3 | In Firebox System Manager, what happens when you press F1 inside a sub-dialog such as Diagnostic Tasks? | Help opens for that dialog | The dialog closes without saving | A packet capture starts | The Firebox restarts | Context-sensitive help follows you into sub-dialogs, which is faster than searching the whole help system for the screen you are on.
3.1 | In Policy Manager, which menu path starts a Fireware OS upgrade? | File > Upgrade | Setup > Global Settings | Tools > Quick Setup Wizard | View > Status Report | The upgrade asks for the admin passphrase and reboots the Firebox onto the new version, so plan it for a maintenance window.
3.2 | After upgrading Fireware OS, why create a new backup image instead of using the one made during the upgrade? | The automatic backup still targets the release you just replaced | The automatic backup is encrypted with a temporary one-time key | The automatic backup only includes the network interface settings | The upgrade deletes the automatic backup after the reboot finishes | A backup image is tied to the Fireware version it was made from. Once the upgrade is done, a restore should use an image made on the version now running.
3.3 | Why add a throwaway Ping policy before restoring the backup? | It gives visible proof that the restore reverted the configuration | The restore fails if the policy list has not changed since the backup | Ping must be allowed for Backup and Restore to reach the Firebox | It creates a checkpoint that the restore uses as its starting point | A restore that appears to succeed proves nothing on its own. A known change that disappears afterwards shows the backup really was applied.
3.4 | How do you confirm the restore actually worked? | Reopen Policy Manager and check the Ping policy is gone | Check that the Firebox rebooted without any errors | Confirm the backup file's size matches what was saved before | Ping the Firebox from the management PC | The only reliable check is the configuration itself. A clean reboot or a reachable device shows the Firebox is up, not that it is running the restored settings.
4.1 | Besides the log server's address, what must you enter in the logging settings to connect to Dimension? | The authentication key from the Dimension setup wizard | The Firebox feature key used for its licensed services | The admin passphrase for Dimension's web interface | A certificate from the Firebox Certificate Portal | The authentication key must match on both sides. A mismatched key is the most common reason logging never connects even though the address is right.
4.2 | Where on the Firebox side can you confirm it is connected to the log server? | Firebox System Manager's Front Panel | The filter bar at the top of Traffic Monitor | Help > About WatchGuard | The policy list shown in Policy Manager | The Front Panel detail pane, or the Log Configuration section of the Status Report, shows whether the log server is Connected.
4.3 | What confirms from the Dimension side that it is receiving this Firebox's logs? | Yes in the Logging column on the Devices tab | A green status tile on the Executive Dashboard | A Connected entry in Traffic Monitor | The Firebox listed on the Scheduled Reports page | Check both ends. The Firebox can report Connected while Dimension still does not show the device as logging, and each view catches different faults.
5.1 | Where does the WatchGuard Cloud verification code come from? | WatchGuard Cloud, when you add the device | The feature key file installed on the Firebox | The Quick Setup Wizard summary page | The Certificate Portal on the Firebox, port 4126 | Enrollment starts in the cloud under Monitor > Fireboxes > Add Device, which generates the code. The Firebox then presents it to prove it is the device you added.
5.2 | Where in Policy Manager do you enter the verification code? | Setup > WatchGuard Cloud | Setup > Logging | File > Upgrade | Setup > Authentication | Enabling WatchGuard Cloud in Policy Manager and saving to the Firebox prompts for the code, which completes enrollment from the device side.
5.3 | What shows that enrollment is complete? | Firebox and WatchGuard Cloud both report Connected | The verification code disappears from the cloud portal | Policy Manager saves the change to the Firebox without errors | The Firebox reboots into cloud-managed mode | Confirm both sides agree: Firebox System Manager's detail pane shows WG Cloud Connected, and the device's Summary page in WatchGuard Cloud shows the same.
6.1 | Why start a continuous ping and note its interface in Traffic Monitor before changing any routes? | So you can see the traffic move once the route changes | Because routes cannot be saved while the link is idle | To keep the external interface from going to sleep | So Traffic Monitor learns the route to 8.8.4.4 first | A baseline turns the change into something you can observe: the same flow, first on one interface and then on another.
6.2 | Which interface type is the DMZ interface given in this lab? | Optional | Trusted | External | Bridge | A DMZ is an Optional interface. It sits in its own zone with its own policies, separate from both the Trusted LAN and the External side.
6.3 | Which kind of route sends only traffic for 8.8.4.4 through 192.168.10.200? | A host route to 8.8.4.4 | A network route to 8.8.0.0/16 | A default route through the DMZ | A dynamic NAT rule for 8.8.4.4 | A host route matches a single address. Because it is more specific than the default route it wins for 8.8.4.4 and leaves every other destination alone.
6.4 | Where can you confirm the Firebox has installed the new host route? | The IPv4 routes in the Status Report | The DHCP leases on the trusted interface | The Quick Setup Wizard summary page | The model field on the Device Status tab | The routing table is the source of truth for what the Firebox will do. Traffic Monitor then confirms the ping really leaves through the DMZ interface.
7.1 | Which address does the secondary Firebox use on the site-to-site link? | 10.0.100.2/24 | 10.0.100.1/24 | 10.0.20.1/24 | 10.0.1.1/24 | The shared link is 10.0.100.0/24, with the primary at .1 and the secondary at .2. 10.0.20.1 is the secondary's trusted interface.
7.2 | On the secondary Firebox, which gateway does the route to 10.0.1.0/24 use? | 10.0.100.1 | 10.0.20.1 | 10.0.1.1 | 192.168.10.1 | A route's gateway must be a next hop the Firebox can reach directly. The primary's address on the shared link is; its trusted address 10.0.1.1 is not.
7.3 | Why does the primary also need a route to 10.0.20.0/24? | Replies need a route back to the secondary's network | Routes only work when both Firebox models are identical | The primary will not answer ARP without a matching route | The site-to-site link stays down until both routes exist | Routing works one direction at a time. With a route on only one side, packets arrive but replies have nowhere to go, which looks like a one-way failure.
7.4 | Which test shows that traffic to 10.0.20.1 passes through both Fireboxes? | tracert from a host on the primary's trusted LAN | nslookup against the primary Firebox's DNS resolver | A speed test run across the new site-to-site link | ipconfig /all on the management PC | tracert lists every hop, so you see the path go through the primary and across the link rather than only knowing the ping succeeded.
8.1 | What is the continuous ping to 1.1.1.1 for in this lab? | Traffic to watch when the SD-WAN action applies | It keeps the link monitor probes from timing out early | It warms the DNS cache before probes are added | It must be running before the SD-WAN tab will open | Like the routing lab, a flow that is already running lets you observe the effect of the change instead of assuming it.
8.2 | Which probe is selected to measure loss, latency and jitter in this lab? | The DNS probe to 1.1.1.1 | The ping probe to 8.8.8.8 | The TCP probe on port 80 | The DMZ next-hop probe | SD-WAN decisions use the measurement probe you select. Here that is the DNS query to 1.1.1.1 for watchguard.com; the ping and TCP probes only check reachability.
8.3 | Which next hop is set for the DMZ interface in Link Monitor? | 192.168.10.2 | 192.168.10.1 | 192.168.10.200 | 10.0.100.2 | 192.168.10.1 is the Firebox's own DMZ address. The next hop is the device beyond it, set to 192.168.10.2 in this lab.
8.4 | How does the Ping policy start using the SD-WAN action? | Its routing setting is changed to use the DMZ action | The action is applied globally to all policies at once | Link Monitor switches it automatically when probes fail | The Ping policy is moved to the top of the policy list | An SD-WAN action does nothing until a policy uses it for outbound routing, which lets you steer specific traffic without touching everything else.
8.5 | Why does the baseline ping start timing out once the action applies? | The DMZ probe target has no real responder | SD-WAN actions block ICMP by default | The external interface was removed from Link Monitor | Traffic Monitor stops logging ICMP traffic | This is expected in the lab: the traffic is steered to a path whose next hop is not really there. Remove the action from the Ping policy afterwards to restore normal routing.
9.1 | Why run a speed test before enabling Traffic Management? | To have a number to compare the throttled result against | Traffic Management needs a measured bandwidth to activate | The Firebox calibrates QoS from the first speed test it sees | Speed tests are blocked once Traffic Management is enabled | Without a baseline you cannot tell whether a lower result is your limit working or just a slow moment on the line.
9.2 | Where are traffic management and QoS features turned on for the whole Firebox? | Setup > Global Settings > Networking | Setup > Actions > Traffic Management | Network > Configuration > Link Monitor | File > Backup and Restore | Actions have no effect until the feature is enabled globally, which is a common reason a correctly built action seems to do nothing.
9.3 | The bandwidth actions in this lab are scoped to All policies. What does that mean? | Every policy using the action shares one limit | Each policy gets its own copy of the limit | The action applies only to the Outgoing policy | The action applies only to the proxy policies | With All policies, the policies that use the action share a single allowance. Per policy would give each policy its own copy of the limit.
9.4 | On the outbound policies, which direction gets the 500 Kbps action? | Forward | Reverse | Both | Neither | Forward is the direction the policy describes, which is upload for outbound traffic. Reverse is the return direction, so downloads get the 1 Mbps action.
9.5 | What should you do after confirming the speed is capped? | Disable Traffic Management to restore full bandwidth | Delete the Outgoing policy so its counters reset to zero | Restore the backup image from Lab 3 | Reboot the Firebox to clear the QoS queues | A lab limit left in place quietly slows everything afterwards. Undoing a test change is part of doing the change properly.
10.1 | What is the effect of disabling the Outgoing policy? | Only traffic that another policy allows can leave | All outbound traffic is allowed without logging | The Firebox stops routing between its interfaces | Inbound NAT rules are removed from the config | Outgoing is the broad allow. Without it, anything not matched by a specific policy reaches the implicit deny, which is why DNS and web need their own policies next.
10.2 | Why replace Any-External in the DNS policy's To list with specific server addresses? | Clients can then only use the approved DNS servers | DNS stops working unless servers are listed by address | Any-External cannot be used in a packet filter policy | It lets DNS traffic skip the policy's logging settings | Restricting DNS stops devices, or malware, from using resolvers you do not control, which would bypass any DNS-based protection.
10.3 | How are the HTTP Deny and HTTPS Deny policies aimed at a single domain? | Their To lists use an FQDN such as *.example.com | Their From lists hold the domain's IP addresses | They use a WebBlocker category for that domain | They rely on DNS forwarding to a sinkhole server | An FQDN in the To list lets a packet filter match a domain by name, and the Denied disposition blocks it. Proxies and WebBlocker come later in the course.
10.4 | Which Traffic Monitor filter terms show the block worked? | http and deny | dhcp and allow | ikev2 and src_user | arp and drop | Filtering for the protocol and the deny shows the blocked requests. Browsing a normal site at the same time confirms nothing else broke.
11.1 | Why delete the HTTP Deny and HTTPS Deny packet filters before configuring the proxies? | So the proxies, not the old filters, do the enforcing | Proxies cannot be saved while packet filters exist | The packet filters would decrypt HTTPS traffic first | Deleting them resets the Firebox's certificate store | Leaving the old denies in place would block the traffic before the proxy saw it, so you could never tell whether the proxy rule works.
11.2 | Where is the URL pattern *example* added? | URL Paths in the Default-HTTP-Client action | The To list of the HTTP-proxy policy itself | An exception inside the WebBlocker action | The From list of the DNS packet filter | URL Paths rules in the HTTP-proxy action match the requested URL, which a policy's To list cannot see.
11.3 | What does setting the HTTPS-proxy's no-rule-matched behaviour to Inspect achieve? | Decrypted HTTPS gets the same HTTP-proxy URL rules | HTTPS is blocked unless a certificate is installed | Only the hostname is checked, never the full URL | HTTPS traffic bypasses the proxy for performance | Inspection decrypts the session and hands it to the HTTP-proxy action, so rules that match the full URL also apply inside HTTPS.
11.4 | Why install the Proxy Authority certificate on the management PC? | So the browser trusts certificates the proxy re-signs | So the Firebox can reach the Certificate Portal | So HTTPS from that PC is exempt from content inspection | So the PC can authenticate on port 4100 | During inspection the Firebox presents certificates signed by its Proxy Authority CA. A client that does not trust that CA warns on every site. The certificate portal is on port 4126.
11.5 | What does it prove when a keyword search on an HTTPS search engine is denied? | The proxy is matching inside the encrypted session | The search engine's hostname is on a WebBlocker list | The DNS policy refused the search engine's name lookup | WebBlocker flagged the whole search engine domain | The keyword travels in the URL path, which is encrypted in HTTPS. A deny on the keyword while the site itself still loads shows the proxy decrypted the session.
12.1 | In this lab, what does the cloned Application Control action do to peer-to-peer categories? | Drops traffic in those categories | Allows them but logs each connection | Throttles them to the 500 Kbps action | Redirects them through the HTTP-proxy | Cloning the Global action keeps the default intact and lets you apply stricter settings only to the policies you choose: HTTP-proxy, HTTPS-proxy and DNS here.
12.2 | Which service in this lab blocks connections to known botnet command sites? | Botnet Detection | Geolocation | Traffic Management | Link Monitor | Botnet Detection blocks traffic to and from addresses known to host botnet infrastructure. Geolocation blocks by country instead, and IPS inspects traffic for attack signatures.
12.3 | What does Reputation Enabled Defense add in the HTTP-proxy? | Immediate denial of URLs with a known-bad reputation | Scanning of downloaded files for virus signatures | Blocking of whole website categories such as gambling | Decryption of HTTPS sessions for inspection | RED uses cloud reputation scores to deny known-bad URLs without waiting for a full scan, which is Gateway AntiVirus's job.
12.4 | Which of these services in the lab needs Total Security Suite rather than Basic Security Suite? | APT Blocker | WebBlocker | Gateway AntiVirus | Intrusion Prevention | The lab lists DNSWatch, IntelligentAV, APT Blocker and Data Loss Protection as Total Security Suite services. WebBlocker, Gateway AntiVirus and IPS are part of Basic Security Suite.
12.5 | What is the EICAR test file for? | Triggering antivirus detection without real malware | Measuring download speed through the HTTP-proxy policy | Testing whether clients trust the Proxy Authority CA | Checking that WebBlocker categories have loaded | EICAR is a harmless standard test string that antivirus engines detect on purpose. Traffic Monitor then shows which service denied it, which is the point of the step.
13.1 | What must match exactly when you add an Active Directory security group to authorize? | The group name, including capitalisation | The domain controller's hardware MAC address | The Firebox's hostname in DNS | The group's creation date in AD | The Firebox compares group names as case-sensitive text. A group typed with different capitalisation never matches, so its members are treated as unauthorized.
13.2 | How is the HTTP Allow policy scoped? | From: the AD group; To: the blocked FQDN | From: Any-Trusted; To: Any-External | From: the blocked FQDN; To: the AD group | From: Any; To: the domain controller | A group as the source and a single FQDN as the destination make a narrow exception: only authenticated members reach that one site, and nothing else changes.
13.3 | Why set the default authentication server to the AD domain? | So users sign in against AD without choosing it | So the Firebox can join the Windows domain itself | So that Firebox-DB accounts stop working entirely | So Traffic Monitor shows AD group names | The default server is preselected on the authentication page, which stops users failing because they tried AD credentials against Firebox-DB.
13.4 | On which port do users reach the Firebox authentication page? | 4100 | 4126 | 8080 | 4118 | The authentication portal is on 4100. 4126 is the certificate portal, 8080 the Fireware Web UI, and 4118 a management port.
14.1 | Where do you create a local user and group for authenticating network users? | The Firebox-DB tab under Authentication Servers | The domain setup wizard under Active Directory | Manage Users and Roles for administrators | The group list in the Mobile VPN IKEv2 wizard | Firebox-DB is the Firebox's own database for authenticating network users. Manage Users and Roles controls who can administer the device, which is a different thing.
14.2 | How are the proxy policies changed so that only authenticated users can browse? | Any-Trusted and Any-Optional are replaced by the group | Authentication is enabled on the external interface | The To list is restricted to the authentication page | The Outgoing policy is re-enabled for that group only | A policy whose source is a user group only matches traffic from users who have authenticated as members. Everyone else falls through to other policies.
14.3 | What does automatic redirect do for an unauthenticated browser? | Sends it to the authentication page | Blocks it without showing any page | Signs it in with the last user's session | Routes it through the Mobile VPN tunnel | Without redirect, an unauthenticated user just sees browsing fail and has to know to visit port 4100 on their own.
14.4 | Which log field shows that traffic belongs to a signed-in user? | src_user | dst_ip | disp | rc | After authentication, log entries carry src_user, and the Authentication List tab shows the active session. Both confirm identity-based policy is in effect.
15.1 | Which authentication server does the IKEv2 wizard use in this lab? | Firebox-DB | Active Directory | RADIUS | SAML | The lab authenticates VPN users against Firebox-DB and authorizes the group created earlier, with the wizard's default virtual IP pool for clients.
15.2 | Why add an HTTP-proxy policy scoped to the IKEv2-Users group? | So VPN users get the same web filtering as office users | Because the tunnel will not establish without that policy | So that IKEv2 users can bypass the URL Paths rules | So the client profile becomes available to download | The wizard's Allow IKEv2-Users policy permits the traffic. A proxy policy for that group applies the same inspection, so remote users do not become a gap in web filtering.
15.3 | Where do you download the IKEv2 client profile? | VPN > Mobile VPN > Get Started | System > Configuration File | The Certificate Portal on port 4126 | Dashboard > FireWatch | The profile archive holds the connection settings the client needs, and running its installer registers the VPN connection in Windows.
15.4 | How do you confirm VPN traffic still gets web filtering? | The Lab 11 domain is still denied through the tunnel | The VPN client shows Connected with a virtual IP address | A ping to the Firebox succeeds through the new tunnel | The virtual IP appears in ipconfig output | A connected tunnel proves reachability, not policy. The blocked domain being denied, plus ikev2 entries in Traffic Monitor, shows the traffic is inspected.
16.1 | On the secondary Firebox, what is the BOVPN virtual interface's remote gateway? | 192.168.10.1 | 192.168.10.2 | 10.0.100.1 | 10.0.20.1 | Each side's local gateway is its own DMZ address and its remote gateway is the peer's. The secondary is 192.168.10.2, so its remote gateway is the primary at 192.168.10.1.
16.2 | Why does the VPN route to 10.0.1.0/24 use metric 2? | So it is only used if the metric 1 route fails | So VPN traffic always wins over the link | Because VPN routes must always use an even metric | So the route is shared by both Fireboxes | The lower metric wins. The site-to-site route at metric 1 carries traffic normally, and the BOVPN route at metric 2 takes over only when the better route disappears.
16.3 | On the primary Firebox, which network does its VPN route point to? | 10.0.20.0/24 | 10.0.1.0/24 | 192.168.10.0/24 | 10.0.100.0/24 | Each side routes to the other side's trusted network. The primary's own trusted network is 10.0.1.0/24, so its VPN route is for the secondary's 10.0.20.0/24.
16.4 | Where do you check the BOVPN's statistics once both paths are cabled? | The Front Panel tab in Firebox System Manager | The Device Status tab in WatchGuard System Manager | System > Configuration File in the Web UI | The Executive Dashboard in Dimension | The Front Panel lists the tunnel and its statistics, which confirms it is up even while traffic still prefers the metric 1 route.
16.5 | After unplugging the site-to-site link, what shows the ping failed over to the BOVPN? | Traffic Monitor's destination interface changes | The ping stops until the cable is plugged back in | The BOVPN route's metric changes to 1 | The Firebox reboots onto the backup route | When the metric 1 route disappears the metric 2 VPN route is used. The ping recovers and Traffic Monitor shows the new destination interface.
17.1 | Which port does the Fireware Web UI use in this lab? | 8080 | 4100 | 4126 | 443 | The Web UI is at the trusted IP on port 8080. 4100 is the authentication portal and 4126 the certificate portal.
17.2 | What does FireWatch in the Web UI help you see? | Active connections, filterable by source address | A readable report covering every policy and setting | Scheduled executive summary reports | The installed feature key along with its expiry dates | FireWatch summarises current activity, so you can drill into connections on an interface and filter down to one client.
17.3 | What does Policy Checker tell you? | Which policy would handle a given connection, and why | Whether each policy has been saved to the Firebox yet | How many connections each policy has handled today | Which policies have logging turned off | You enter an interface, protocol, address and port, and Policy Checker shows the policy that would match. It answers which rule wins without sending real traffic.
17.4 | What does Test Connection under Authentication > Servers check for an AD domain? | That the Firebox can query AD with a valid user | That client PCs can reach the domain controller directly | That the AD group names are spelled with the right case | That DNS resolves the domain for every client | The test runs from the Firebox using the credentials you enter, which isolates Firebox-to-AD connectivity from anything on the client side.
18.1 | Which Dimension dashboard focuses on blocked traffic? | Security Dashboard | Executive Dashboard | Policy Map | Log Manager | The Executive Dashboard summarises traffic at a high level. The Security Dashboard shows what policies and security services blocked.
18.2 | What does Policy Map show? | How traffic is distributed across firewall policies | The physical layout of the Firebox's interfaces | The order in which the policies were originally created | Which administrator last edited each policy | Policy Map shows traffic volume per policy, which is a quick way to spot a policy doing far more, or far less, than you expect.
18.3 | Where do you pull a summary report for one client IP address in Dimension? | Per Client Reports > Summary | Executive Dashboard > Top Clients | FireWatch > Source tab | Policy Map > Details | Per Client Reports summarise one address's activity over a period, while FireWatch shows a live view of current connections.
19.1 | Which WatchGuard Cloud dashboard shows allowed traffic? | Executive | Security | Subscription | FireWatch | Executive covers allowed traffic, Security covers blocked traffic, and Subscription covers what the licensed services did.
19.2 | Why compare FireWatch in WatchGuard Cloud with Dimension and the Web UI? | To learn where the same data lives in each tool | Because only one of them can show live connections | To find which tool has the most accurate data | Because FireWatch in Dimension is deprecated | Real support work, and the exam, both involve finding the same information in different management tools, so knowing each location matters.
19.3 | Where is the Interface Summary bandwidth graph in WatchGuard Cloud? | Health > Interface Summary | Dashboards > FireWatch | Administration > Notifications | Scheduled Reports | Health reports cover the device and its interfaces. FireWatch covers connections, and notifications and scheduled reports live under Administration.
20.1 | Where is the alarm enabled for the Lab 11 URL Paths rule? | Proxy and AV Alarms on that rule | Setup > Logging on the Firebox | Scheduled Reports in WatchGuard Cloud | The filter options in Traffic Monitor | The alarm is raised by the rule itself when it matches, and the Firebox sends it on as a notification.
20.2 | In WatchGuard Cloud, what is the notification rule scoped to? | Device Alarms | Scheduled Reports | Subscription Services | Firmware Updates | A rule for Device Alarms, delivered by email, turns alarms raised on the Firebox into messages you actually receive.
20.3 | How do you trigger the alarm to test the notification? | Browse to the blocked URL pattern again | Reboot the Firebox from the cloud | Disable the HTTP-proxy policy briefly | Run the Executive Summary report now | Matching the rule that carries the alarm is the most direct end-to-end test: the rule, the alarm, the cloud rule and email delivery all have to work for the message to arrive.
20.4 | How do you check a scheduled Executive Summary report works without waiting for the schedule? | Run it now for the past 24 hours | Change the schedule to every minute | Export Traffic Monitor to email | Trigger a Device Alarm notification | Running the report on demand proves the report and its delivery work today, instead of finding out tomorrow that nothing arrived.
`;

function parse(rows: string): Record<string, LabCheckpoint> {
  const result: Record<string, LabCheckpoint> = {};
  for (const line of rows.trim().split('\n')) {
    const fields = line.split('|').map(s => s.trim());
    if (fields.length !== 7) throw new Error(`Malformed lab checkpoint: ${fields[0]}`);
    const [key, question, answer, d1, d2, d3, why] = fields;
    if (result[key]) throw new Error(`Duplicate lab checkpoint: ${key}`);
    result[key] = { question, answer, options: [answer, d1, d2, d3], why };
  }
  return result;
}

export const labCheckpoints: Record<string, LabCheckpoint> = parse(ROWS);

export const checkpointFor = (labId: number, stepNumber: number): LabCheckpoint | undefined =>
  labCheckpoints[`${labId}.${stepNumber}`];
