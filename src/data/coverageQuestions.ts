import type { Question } from './questions';
import { localSource, NETWORK_SOURCE, type Track, type SourceReference } from '../engine/types';

const cloudHelp = (page: string): SourceReference => ({
  title: 'WatchGuard Help: cloud-managed Fireboxes',
  url: `https://www.watchguard.com/help/docs/help-center/en-US/Content/en-US/WG-Cloud/Devices/managed/${page}.html`,
});
const basics = localSource('Assessment objectives: networking basics, p. 337');

function questions(start: number, track: Track, topic: Question['topic'], source: SourceReference, rows: string): Question[] {
  return rows.trim().split('\n').map((row, i) => {
    const [objective, question, answer, b, c, d, explanation] = row.split('|').map(s => s.trim());
    if (!explanation || row.split('|').length !== 7) throw new Error(`Invalid coverage question ${start + i}`);
    return { id: start + i, track, topic, objective, question, options: [answer, b, c, d],
      correctAnswer: answer, correctAnswers: [answer], correctAnswersCount: 1, isMultiSelect: false,
      difficulty: 'applied', explanation, sources: [source],
      firewareVersion: track === 'local' ? '12.9.2 study baseline' : undefined };
  });
}

/** Original scenarios added after the objective audit. Stable IDs; measured separately by the audit script. */
export const coverageQuestions: Question[] = [
  ...questions(2000, 'local', 'Proxies', localSource('Content Actions and Routing Actions, pp. 218–219'), `
5.5 Content actions | Two web applications share one public IP and HTTPS port. Requests to /sales and /support on the same hostname must reach different internal servers. Which configuration exposes the information needed to route them? | Inspect HTTPS and match paths in an HTTP content action | Match both paths in the HTTPS domain-name rules directly | Use two SNAT actions that match the same public socket | Select a server by the client source port in dynamic NAT | The URL path is inside encrypted HTTP. Inspection exposes it to an HTTP content action, whose routing rules can select a backend. Domain-name rules cannot read encrypted paths; identical SNAT matches cannot distinguish them, and source ports identify client sessions rather than application paths.
5.5 Content actions | An inbound HTTPS-Server policy must route a known SNI hostname to a backend while leaving TLS encrypted to that backend. Which setting provides the routing decision? | A domain-name rule with a routing action | An HTTP content rule that matches the URL path | A client proxy action that rewrites the Host header | A TLS offload action using HTTP to the backend | A routing action in the HTTPS-Server domain-name rule selects the server and port without inspecting the encrypted HTTP body or path. Path matching and Host-header handling need readable HTTP. TLS offload would decrypt on the Firebox and use clear-text HTTP to the backend, contrary to the requirement.
`),
  ...questions(2010, 'local', 'Network Services', basics, `
1.2 NAT basics | Two internal clients connect to the same public server on TCP 443 through one public IPv4 address. Their original source ports are identical. What lets the NAT device deliver replies to the correct client? | Distinct translated ports with stored session mappings | Separate destination ports on the public web server | Separate public source addresses for both clients | Separate HTTP hostnames stored in the ARP cache | Port translation can allocate distinct outside source ports even when inside ports collide. The device retains each mapping for replies. The server still listens on 443, a second public address is unnecessary, and ARP resolves local link addresses rather than HTTP names.
1.3 Packet headers | A packet with IPv4 TTL 1 arrives at a router and needs another routed hop. No other filter blocks it. What happens at this router? | It drops the packet and normally sends ICMP Time Exceeded | It forwards the packet and resets TTL to the original value | It delivers the packet after changing the TCP window size | It asks the destination host to renew the packet lifetime | Routing decrements TTL; reaching zero prevents forwarding and normally produces an ICMP Time Exceeded response. Routers do not renew the sender's hop limit. TCP windows govern flow control, not the lifetime of an IP packet, and the destination cannot renew a packet it has not received.
1.4 MAC addresses | A host sends an IP packet to a server on another subnet through its default gateway. The gateway's MAC is known. Which destination MAC belongs in the host's first Ethernet frame? | The default gateway interface MAC | The remote application server MAC | The Ethernet broadcast destination | The host's own network adapter MAC | Ethernet addresses identify the next hop on the local link. The host sends the frame to its gateway while the IP destination remains the remote server. The remote server's MAC is not reachable on this link; broadcast is unnecessary once the gateway MAC is known, and the host's MAC is the source.
`),
  ...questions(2100, 'network-plus', 'Network Architecture', NETWORK_SOURCE, `
1.1 OSI | A switch forwards frames correctly within a VLAN. A router between VLANs receives those frames but has no route to the destination subnet. At which OSI layer is the demonstrated forwarding failure? | Layer 3, network | Layer 2, data link | Layer 4, transport | Layer 7, application | The missing information is an IP route, a Layer 3 decision. Successful local frame delivery gives evidence for Layer 2 on that path. Neither a transport port nor an application request can repair the absent route, though separate faults at those layers could exist.
1.2 Appliances | A public application runs on three healthy web servers. Clients must use one service address, and new requests must stop going to a server when its health check fails. Which device function meets both needs? | A load balancer using live backend health checks | A Layer 2 switch with spanning tree enabled | A NAT gateway with one fixed inbound mapping | A passive IDS inspecting mirrored web traffic | A load balancer presents the service and chooses healthy backends. Spanning tree prevents switching loops, a single fixed NAT mapping does not select among healthy servers, and a passive IDS observes traffic without distributing requests.
1.2 Appliances | Analysts need to detect suspicious traffic on a mirrored switch port. The sensor must not be able to interrupt the production path. Which deployment meets that constraint? | A passive intrusion detection sensor | An inline intrusion prevention appliance | A reverse proxy terminating client TLS | A routed firewall between users and servers | A passive IDS receives a copy of traffic and can alert without sitting in the forwarding path. An inline IPS, reverse proxy, or routed firewall participates in delivery and can interrupt it. Detection and prevention are different deployment goals, even if a product supports both modes.
1.6 Architecture | Most traffic in a new data centre runs between servers in different racks. The design needs predictable paths and equal-cost uplinks from each rack to a shared switching tier. Which architecture best fits? | Spine and leaf with routed equal-cost paths | A single hub with every server in one collision domain | A daisy chain linking each rack to the next rack | A ring that sends every flow through a central firewall | A spine-and-leaf fabric gives each leaf connections to the spine tier, supporting multiple equal-cost paths for east-west traffic. A hub shares collisions, a chain creates unequal path lengths, and forcing every flow through one firewall contradicts the requested path structure.
1.8 Modern networks | Engineers commit intended switch settings to a repository. A nightly job compares running settings with that approved version and finds an undocumented VLAN. What has the job detected? | Configuration drift from the approved settings | A routing loop in the physical underlay | Exhaustion of the IPv4 address allocation | An expired device authentication certificate | Drift is a difference between intended and running configuration. Versioned definitions make that difference reviewable. A configuration comparison alone does not demonstrate looping packets, address exhaustion, or certificate expiry; each would require separate evidence.
`),
  ...questions(2110, 'network-plus', 'Network Operations', NETWORK_SOURCE, `
3.5 Management | A routing mistake has made the production IP network unreachable. Engineers still have a separate management circuit to a console server connected to the router's serial port. Which recovery path uses that design? | Out-of-band access through the console server | In-band SSH through the failed production route | A new VPN carried over the failed production route | A browser session to the router's production address | Out-of-band management uses a path independent of production forwarding, here a separate circuit and serial console. The other choices all depend on the broken IP path. This is why access-path independence matters more than simply changing the management protocol.
3.5 Management | Administrators need remote SSH access to switches, but policy forbids direct connections from user workstations to the management subnet. A hardened host is permitted into that subnet. Which approach fits? | Connect through the approved jump host | Expose each switch SSH port through public NAT | Move switch management onto the user VLAN | Permit workstation SSH directly in the subnet ACL | A jump host supplies a controlled intermediate management path. Public NAT widens exposure, moving management loses the boundary, and permitting direct workstation access defeats the stated rule. The jump host still needs individual access controls and audit records; its existence is not authorization by itself.
`),
  ...questions(2200, 'cloud', 'IP Addressing', basics, `
1.1 Addressing | A cloud-managed branch needs 50 usable IPv4 addresses on one LAN, including its gateway. Which smallest conventional subnet accommodates the branch? | /26, with 62 usable addresses | /27, with 30 usable addresses | /25, with 126 usable addresses | /24, with 254 usable addresses | Six host bits provide 64 addresses, of which 62 are conventionally usable. A /27 is too small. A /25 or /24 works but wastes more space and fails the smallest-subnet requirement. Cloud management does not change IPv4 subnet arithmetic.
`),
  ...questions(2201, 'cloud', 'Network Services', basics, `
1.2 NAT | Branch hosts use private IPv4 addresses and share a single public address for outbound Internet sessions. Which translation behavior permits simultaneous sessions? | Translate source addresses and track source ports | Translate destination addresses to private servers | Replace remote MAC addresses with public IP addresses | Assign each session a separate public IPv4 network | Source address and port translation distinguish outbound sessions sharing one public address. Destination translation publishes internal services instead. MAC addresses and IP addresses belong to different forwarding layers, and allocating a public network per session is not what shared-address NAT does.
1.3 Headers | A capture at a cloud-managed branch shows a client SYN, a server SYN-ACK, and then repeated client SYNs without a final ACK. Which conclusion is supported at this capture point? | The TCP handshake has not completed | The HTTP server has returned an error response | The TLS certificate has failed trust validation | The DNS resolver has rejected the server hostname | TCP needs the final acknowledgement to complete this handshake. Repeated SYNs show retry behavior, not a completed HTTP or TLS exchange. A hostname rejection is a DNS event and cannot be established from these TCP flags alone.
1.4 MAC | A branch client has the correct IP and route but sends frames for its gateway to an old MAC after a gateway replacement. Which cached information should be checked first? | The cached IPv4-to-MAC neighbor mapping | The hostname-to-IPv4 resolver cache | The list of trusted root certificates | The saved mobile VPN user credentials | ARP caches the local IPv4 neighbor's MAC, so a stale gateway mapping directly explains the wrong Ethernet destination. DNS caches names, certificates establish trust, and VPN credentials authenticate users; none controls this local frame address.
1.5 Ports | A branch allows UDP 53 to its resolver. Captures show truncated DNS replies followed by denied TCP 53 attempts. Which targeted rule change supports the retry? | Permit TCP 53 to that same resolver | Permit TCP 443 to every external address | Permit UDP 67 to that same resolver | Permit UDP 123 to the branch gateway | DNS can retry truncated responses over TCP 53. The destination can remain restricted to the approved resolver. HTTPS, DHCP, and NTP use the other proposed ports and do not carry this observed retry. Allowing an unrelated broad rule would not repair the demonstrated path.
`),
  ...questions(2210, 'cloud', 'Routing', cloudHelp('network_config_routes'), `
3.3 Static routes | A cloud-managed Firebox reaches a downstream router at 10.8.1.254 on its internal /24. That router connects 10.8.2.0/24. Policies allow the traffic, but the Firebox lacks a route to that subnet. Which route belongs on the Firebox? | 10.8.2.0/24 via 10.8.1.254 | 10.8.1.0/24 via 10.8.2.254 | 0.0.0.0/0 via the ISP gateway | 10.8.2.0/24 via its own internal IP | A static route pairs the destination prefix with a reachable next-hop router. The downstream router supplies the missing path. Reversing the networks describes the wrong destination, the ISP default sends private traffic away from the branch, and the Firebox cannot use itself as the next hop.
`),
  ...questions(2211, 'cloud', 'NAT', cloudHelp('firewall_snat'), `
3.4 NAT configuration | A cloud-managed Firebox must publish public TCP 8443 to an internal server listening on 443. The inbound policy matches 8443. What must its Static NAT destination specify? | The internal server and destination port 443 | The internal server and source port 443 | The public client and destination port 8443 | The public client and source port 8443 | The forwarding action selects the internal destination and can rewrite its destination port. Rewriting the source port does not make the server listen on 8443. Using the public client as the destination reverses the flow. The NAT action must be used by the inbound policy; saving an unused action grants no access.
`),
  ...questions(2212, 'cloud', 'Network Services', cloudHelp('network_config_dns'), `
3.2 Name services | Public names resolve through a branch Firebox, but names in corp.example exist only on an internal DNS server. The branch should send that domain's queries to the internal server while keeping public resolution unchanged. What configuration fits? | A conditional DNS forwarding rule for corp.example | A public default route pointing at the DNS server | A static NAT mapping for every internal hostname | A longer cache lifetime for failed DNS responses | Conditional forwarding chooses a resolver for a specific domain. A route moves packets but does not select a DNS authority, NAT translates addresses rather than names, and caching a failure longer preserves the failure. Confirm the internal DNS server is reachable as well as selecting it.
`),
  ...questions(2213, 'cloud', 'Switching & Wireless', cloudHelp('networks_config_summary'), `
3.6 VLANs | A cloud-managed Firebox network is assigned VLAN 40 with tagged traffic on the switch uplink. The switch permits only VLAN 30 there. Both physical ports are up, but VLAN 40 clients cannot reach the Firebox. What should be aligned? | The tagged VLAN allowed on both uplink ends | The DNS suffix assigned to both network segments | The TLS certificate installed on each client device | The outbound NAT address used for Internet traffic | A working physical link does not prove that the required VLAN crosses it. The switch must carry the tag the Firebox network uses. DNS, certificate trust, and Internet source NAT operate after this local connectivity requirement and cannot restore a missing VLAN on the uplink.
`),
  ...questions(2214, 'cloud', 'Network Services', cloudHelp('network_config_internal'), `
3.5 DHCP | A branch LAN uses a DHCP server on another routed subnet. Clients send Discover broadcasts, but the server sees none. Routing and policies are otherwise correct. Which network function is missing at the client subnet boundary? | DHCP relay to the server address | Static NAT to the client broadcast address | A DNS forwarder for the server hostname | A larger lease pool on the remote server | Routers do not ordinarily forward a client's local DHCP broadcast. A relay sends the request to the server and identifies the client subnet. NAT and DNS do not relay DHCP discovery; enlarging the server pool does not help a request that never arrives.
`),
  ...questions(2220, 'cloud', 'WatchGuard Cloud', cloudHelp('configure_tls_decryption'), `
4.3 TLS inspection | Decrypt HTTPS Traffic is enabled on a branch's outbound policy. Managed browsers now reject otherwise valid sites because the re-signed certificate issuer is unknown. What restores trust while retaining inspection? | Distribute the Firebox proxy authority certificate | Disable certificate verification in every browser | Replace the branch DNS server with a public resolver | Allow the traffic in a policy without decryption | Inspection re-signs traffic with the Firebox's proxy authority. Clients must trust that authority, which can be distributed centrally. Disabling validation removes a security check, DNS does not repair certificate trust, and bypassing decryption violates the requirement to retain inspection.
`),
  ...questions(2221, 'cloud', 'WatchGuard Cloud', cloudHelp('firewall_policies_priority'), `
4.2 Precedence | A cloud-managed Firebox uses Automatic Policy Order. A First Run deny and a Core allow both match a new connection; the Core rule has a more specific source. Which outcome follows? | The First Run policy denies the connection | The Core policy allows the connection | The last policy edited decides the outcome | Both policies combine to permit the connection | Policy groups are compared before specificity within a group: First Run precedes Core. A more specific Core rule therefore does not override this First Run match. Edit time is not the priority rule, and matching policies do not combine their actions.
`),
  ...questions(2230, 'cloud', 'WatchGuard Cloud', cloudHelp('auth_domains'), `
5.2 Authentication domains | An Active Directory authentication domain exists in the WatchGuard Cloud account, but a newly onboarded Firebox cannot use its groups in policy. What device-level configuration is still needed? | Add the shared authentication domain to the Firebox | Add the directory users as Cloud operator accounts | Convert the Firebox to local configuration management | Add a Static NAT action for the directory server | An account-level AD domain must also be added to the Firebox configuration before its users and groups can be referenced there. Cloud operators administer the platform, not ordinary network-user access. Changing management mode or creating NAT is not the missing association.
`),
  ...questions(2231, 'cloud', 'Mobile VPN', cloudHelp('mvpn_configure'), `
5.5 Mobile VPN | A cloud-managed IKEv2 deployment uses split tunneling. An administrator adds a private subnet to allowed resources and deploys the change, but existing clients still have the old route list. What should the administrator distribute? | Updated IKEv2 VPN client configuration files | A new WatchGuard Cloud operator invitation | An exported Firebox feature key file | A backup image from the remote gateway | Changes to IKEv2 allowed resources require updated client configuration files on user devices. Device deployment alone does not replace an installed client profile. Operator invitations, feature keys, and gateway backups do not carry the client route configuration.
`),
  ...questions(2232, 'cloud', 'BOVPN', cloudHelp('bovpn_cloud-cloud'), `
5.6 Site VPN routes | Two cloud-managed Fireboxes share a BOVPN. A new branch subnet is absent from the headquarters routing table. Which BOVPN change supplies its route at headquarters? | Add the branch subnet as a branch network resource | Add the branch subnet as a headquarters external network | Add the headquarters public IP as a branch DNS server | Add the branch subnet to headquarters DHCP exclusions | An endpoint's network resources become routes through the BOVPN on its peer. External networks choose gateway connectivity; DNS servers resolve names, and DHCP exclusions reserve lease addresses. None of those substitutes for the missing destination route.
5.7 VPN NAT | Two cloud-managed sites in one account both use 192.168.1.0/24 and cannot renumber. Their BOVPN must distinguish the sites. Which design addresses the overlap? | Map each site's subnet to a distinct VPN NAT range | Give both sites the same translated VPN subnet | Add identical local and remote routes without translation | Change only the public IP addresses of the VPN gateways | Distinct translated ranges remove the ambiguity between overlapping private networks. The two translations must be coordinated. Identical translated ranges preserve the collision, unmodified routes cannot distinguish the networks, and changing outer public endpoints does not change the overlapping inner addresses.
`),
];


