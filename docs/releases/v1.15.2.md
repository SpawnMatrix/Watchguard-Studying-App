# Version 1.15.2 — Fix a stale route-question answer string

Question id 8 (routing multi-select: which two static routes reach a server behind a downstream router) carried a legacy `correctAnswer` display string that read "Route to 10.0.20.80, Gateway 192.168.10.5" — missing the `/32` prefix present in the actual option text and in the `correctAnswers` array that grading and the UI actually use.

- Grading was never affected: `gradeQuestion` and the option-highlight UI both read `correctAnswers` (the array), which already had the correct `/32` text. This was a dead, unused field.
- Fixed anyway so the two fields don't disagree — found while sampling authored and legacy question content for the ambiguous-distractor issue raised earlier; this was the only inconsistency found across the sample, and it never reached a learner.

No migration, no data-contract change. A one-line content fix.
